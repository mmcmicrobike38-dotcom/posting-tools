# Automation App

Automation orchestration entrypoints live here as the system grows beyond a single desktop runtime.

The current Python bridge remains in `scripts/python_bridge.py` and imports business logic from `core/` and `python_backend/`.

