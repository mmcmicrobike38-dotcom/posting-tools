# src-tauri/config

Reserved for Tauri-specific configuration overlays.

Production Google credential files are not bundled with the app. Deploy them outside the packaged application, preferably in:

`%LOCALAPPDATA%\SIMSOFT Posting\config\`
