# Posting Frontend Feature

Target structure:

```text
presentation/
  pages/
  components/
  view-models/
application/
  postingFacade.ts
  postingStore.ts
api/
  postingApi.ts
model/
  postingTypes.ts
```

The current dashboard flow should migrate here incrementally while `useSimsoftDashboard` remains as a compatibility composer.
