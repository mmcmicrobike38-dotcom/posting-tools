# Renderer API Boundary

All renderer code should call backend capabilities through this folder.

`window.simsoft` access should be isolated here so feature view models can be tested with mocked API clients.
