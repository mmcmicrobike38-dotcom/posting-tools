# Shared UI

Reusable UI primitives live here:

- modal system
- table system
- form engine
- buttons, inputs, layout primitives

Feature-specific UI stays inside `src/renderer/features/<feature>/presentation`.
